const config = {
    database: "car_rental",
    user: "root",
    password: "53037912Aa*",
    host: "sql",  // Change this to 'mysql' if using Docker and that's the container name
    port: 3306
};

module.exports = config;
